#include "bits/stdc++.h"
#define BUFFER_SIZE 1024
using namespace std;
typedef pair<int, int> button;
typedef pair<int, int> prize;
int main() {
    FILE* input = fopen( "input.txt", "r" );
    char buf[BUFFER_SIZE + 1] = "\0";
    int lineCnt = 0;
    button a, b;
    prize p;
    while( !feof( input ) && fgets( buf, BUFFER_SIZE + 1, input ) ) {
        
    }
}